# OWNER_MANUAL_TEST_LOG.md — POLYTSIA V1.6.2, #171

Консолідований журнал ручних перевірок власника продукту, який досі не мав єдиної точки входу:
до цієї фази кожен UI-чутливий milestone породжував свій окремий `*_MANUAL_TEST.md` без спільного
індексу (`docs/CALENDAR_VISUAL_MANUAL_TEST.md`, `docs/MANUAL_UX_TEST_V1_6.md`), і жоден з них не
зібраний в одному місці. Це середовище розробки не має фізичного пристрою (весь код писався й
перевірявся через `tsc`/`jest`/`eslint`/CI та ручний рев'ю коду, без емулятора чи реального
iPhone/Android) — тож усе нижче є чек-листом для живого прогону, не звітом про вже пройдені
перевірки.

**Do not mark PASS yourself.** Жодна клітинка "Пройдено" в усьому цьому документі (і в жодному з
документів, на які він посилається) не позначена й не повинна бути позначена ким-небудь, окрім
живого прогону на реальному пристрої власником продукту.

**Пристрій:** _____________________ **ОС/версія:** _____________________ **Дата:** _____________

Що НЕ входить сюди: повний наскрізний регрес усього продукту (усі milestone'и, не лише V1.6.2) —
це роль `docs/FINAL_OWNER_ACCEPTANCE_FLOW.md`-подібного документа, який готується в #172 (quality
gate + фінальний звіт), не тут. #171 — лише документація й консолідація, продуктового коду не
змінює.

## 0. Раніше створені чек-листи — статус досі PENDING OWNER CHECK

Ці документи вже існували до #171, охоплюють UI, який не змінювався відтоді, і в git-версії їхні
клітинки "Пройдено" лишаються непозначеними — тобто досі не пройдені живим прогоном, наскільки
видно з репозиторію:

- `docs/CALENDAR_VISUAL_MANUAL_TEST.md` — 12 пунктів, Календар — візуальна композиція дня
  (стек обкладинок, бейдж "+N", інтенсивність).
- `docs/MANUAL_UX_TEST_V1_6.md` — 17 пунктів, наскрізні UX-фікси V1.6.

Якщо власник продукту вже фізично прогнав ці два документи на пристрої окремо від git — цей
рядок про це не знає (клітинки в репозиторії не позначені); статус вище варто звірити з живою
пам'яттю власника, а не лише з цим журналом.

## 1. #167 — Сезони читання: редизайн на "емоційну пам'ять"

Сезон (`app/seasons/[seasonKey].tsx`) перебудований з дашборду статистики на емоційний спогад
(`docs/READING_SEASONS.md`).

- [ ] Відкрити сезон із завершеними книгами — головний вміст картки: обкладинки прочитаних книг,
  улюблена книга, цитата, збережені думки з щоденника. Компактні цифри (макс. 4) можуть бути, але
  НЕ як головний вміст — це не має виглядати як ще один звіт поруч зі Statistics/Wrapped.
- [ ] Сезон, що охоплює межу року (напр. грудень 2026 — лютий 2027) — підпис "Зима 2026/27", НЕ
  "Зима 2027".
- [ ] Поширити картку сезону (share-зображення) — у зображенні немає приватного тексту нотаток
  щоденника, лише навмисно винесений вміст (цитата/думка, якщо вона мала бути показана).
- [ ] Книга, прочитана в межах сезону, але чий поточний `UserBook.status` зараз інший (напр.
  зараз перечитується) — все одно коректно прив'язана до ТОГО сезону, коли реально читалась
  (джерело — `ReadingRun`, не поточний статус).

**Пройдено:** [ ] Так [ ] Ні — **Нотатки:** ______________________________________________

## 2. #169 — Home: приховування контекстної картки

`src/components/home/HomeContextCard.tsx` — кожен з п'яти видів картки отримав кнопку "×".

- [ ] На Home з'явилась контекстна картка (будь-який з 5 видів: застояне читання, капсула
  очікує, "цей день", ціль близько до завершення, TBR-пропозиція) — тап "×" одразу ховає САМЕ ЦЮ
  картку; або з'являється наступна за пріоритетом, або секція коректно зникає — не лишається
  порожній/поламаний слот.
- [ ] Прихована картка НЕ з'являється повторно того самого дня при перезаході на Home.
- [ ] (За можливості змінити системну дату на пристрої) наступного календарного дня та сама
  картка знову може з'явитись — приховування не постійне.
- [ ] Book Details книги, яка АКТИВНО читається/перечитується, але вже має капсулу з попереднього
  завершеного прочитання — картка "Переглянути мою пам'ять про цю книгу" присутня й веде в Book
  Memory hub (раніше єдиний шлях туди був лише для `finished`/`rereading`).

**Пройдено:** [ ] Так [ ] Ні — **Нотатки:** ______________________________________________

## 3. Фаза 1 — Старт читання: узгодження статусу з `reading_run`

`ReadingSessionRepository.start()` тепер атомарно виставляє статус книги разом зі стартом сесії.

- [ ] Почати читання книги зі статусом "Хочу прочитати" — статус одразу стає "Читаю".
- [ ] Почати перечитування вже завершеної книги — статус стає "Перечитую" (не "Читаю").
- [ ] Продовжити книгу зі статусом "Не дочитав" (новий прохід) — статус стає "Читаю" (НЕ
  "Перечитую" — рішення власника продукту: книга ще жодного разу не дочитана до кінця).
- [ ] Після кожного з трьох кроків вище — "Перевірка даних" (`/data-doctor`) не показує
  "суперечливий статус" для цієї книги.

**Пройдено:** [ ] Так [ ] Ні — **Нотатки:** ______________________________________________

## 4. Фаза 2 — Скасування сесії: коректне повернення статусу

`computeStatusAfterRunRemoval()` — скасування щойно створеної спроби читання тепер прибирає й сам
run, і повертає книзі статус, який був би, якби цієї спроби не існувало.

- [ ] Почати нову сесію читання (новий прохід), одразу натиснути "Скасувати сесію" на активному
  екрані, не зберігаючи прогресу — книга повертається до статусу ДО старту (або "Хочу
  прочитати", якщо іншого живого проходу не було), сесія й прохід зникають з історії.
- [ ] Регресія: книга з реальною історією (прохід із кількома сесіями або вже завершений прохід)
  — скасування ПОТОЧНОЇ активної сесії НЕ стирає й не чіпає попередню завершену історію проходів.

**Пройдено:** [ ] Так [ ] Ні — **Нотатки:** ______________________________________________

## 5. Фаза 3 — Прогрес відновлення бекапу + автоперевірка цілісності

`BackupRepository.restoreAll` тепер звітує реальний прогрес; одразу після відновлення —
одноразова автоматична перевірка цілісності.

- [ ] Відновити бекап (JSON) — індикатор прогресу показує реальний відсоток (не невизначений
  спінер) протягом усього відновлення ~38 таблиць.
- [ ] Одразу після завершення відновлення — якщо перевірка цілісності знаходить проблеми,
  з'являється `Alert` з переходом на "Перевірку даних"; якщо проблем нема — жодного зайвого
  сповіщення.
- [ ] Після цього — повторний вхід у Профіль (без нового відновлення) НЕ запускає перевірку
  цілісності повторно автоматично.

**Пройдено:** [ ] Так [ ] Ні — **Нотатки:** ______________________________________________

## Свідомо не включено

`#165` (repository test coverage), `#168` (analytics performance), `#170` (versioning/npm
audit/CI/залежності) — жодних UI- чи поведінкових змін для користувача, лише внутрішній код,
тести й документація; ручна перевірка для них не застосовна.
