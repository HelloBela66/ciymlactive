node_modules/
.expo/
dist/
web-build/
expo-env.d.ts
*.orig.*

# Native
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# macOS
.DS_Store

# Env
.env
.env.local
.env.admin

# Logs
npm-debug.*
yarn-debug.*
yarn-error.*

# Local SQLite artifacts (if ever dumped for debugging)
*.db
*.sqlite

# Testing
coverage/

# Supabase CLI local/machine-specific state (project-ref, pooler URL, cached versions —
# не секрети, але й не частина джерела; регенеруються `supabase link`/`functions deploy`)
supabase/.temp/

# Десктоп-застосунок Claude (Cowork) сам створює цю теку в корені проєкту — плоскі копії
# щойно створених файлів на момент збереження на пристрій. Не частина джерела, не варто
# комітити (і псує Jest — див. коментар біля testPathIgnorePatterns у jest.config.js).
/Claude outputs/
