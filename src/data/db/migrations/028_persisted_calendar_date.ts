import type { SQLiteDatabase } from 'expo-sqlite';

/**
 * Migration 028 — PERSISTED CALENDAR IDENTITY (POLYTSIA V1.7, Phase 11, ТЗ §9).
 * Повне обґрунтування й план — `docs/V1_7_TEMPORAL_IDENTITY_IMPACT.md`; тут стисло, що саме
 * робить ця міграція і чому саме так.
 *
 * ── НАВІЩО ───────────────────────────────────────────────────────────────────────────────────
 * Досі локальний календарний день ніде не зберігався — він обчислювався щоразу заново з поточного
 * поясу пристрою. Поки пояс не змінюється, це правильно (перехід на літній час теж коректний:
 * `Date` застосовує історичні правила поясу для того самого моменту). Але коли змінюється САМ
 * пояс — переїзд, виправлення неправильно виставленого поясу, ретроактивна правка IANA-правил —
 * історія їде: сесія, що почалась 1 червня о 00:30 у Києві, після переїзду в UTC−5 стає 31 травня,
 * і разом із нею їдуть підсумки місяців, активні дні, «Цей день у твоєму читанні» й дати віх.
 *
 * ── ЖОДНОГО BACKFILL (product decision власника) ─────────────────────────────────────────────
 * Справжній offset на момент запису НІКОЛИ не зберігався, тож для наявних рядків його нема звідки
 * взяти. Backfill міг би спиратись лише на поточний пояс — тобто записав би сьогоднішню здогадку у
 * вигляді, невідрізненному від зафіксованого факту. Після цього «тут справді був UTC+3» і «ми так
 * припустили у 2026-му» стали б однаковим числом у колонці.
 *
 * Тому для всіх наявних рядків значення лишається `NULL`, і `NULL` означає рівно «ми не знаємо».
 * Read-path для таких рядків використовує задокументований fallback (конвертація з абсолютного
 * моменту в поточному локальному календарі), і це називається **legacy reconstructed calendar
 * attribution**, а не historical persisted truth.
 *
 * ── ЧОМУ ДАТА, А НЕ OFFSET ───────────────────────────────────────────────────────────────────
 * Продуктова вимога §9 — стабільна атрибуція періоду: до якого читацького ДНЯ належала подія.
 * Для цього достатньо самої дати; абсолютний момент і далі зберігається поруч у `*_at`. Жодних
 * IANA-зон, UTC-offset чи історії поясів не додається (ТЗ §4) — якщо колись знайдеться конкретна
 * вимога, якої дата не покриває, її спершу треба задокументувати, а вже потім розширювати схему.
 *
 * ── ПРАВИЛО НАЗВ ─────────────────────────────────────────────────────────────────────────────
 * `X_calendar_date` — збережена локальна календарна дата колонки `X_at` (`YYYY-MM-DD`). Це одне
 * правило замість трьох окремих назв, і воно робить неможливим сплутати дату з моментом: у цій
 * схемі будь-який час — це завжди `*_at`.
 *
 * ── SCOPE МІНІМАЛЬНИЙ (ТЗ §2) ────────────────────────────────────────────────────────────────
 * Лише три колонки: старт сесії, старт проходу, завершення проходу — саме те, від чого залежить
 * атрибуція періодів. `note`/`quote`/`user_book` та решта timestamped-таблиць СВІДОМО не чіпаються.
 *
 * Простий `ALTER TABLE ... ADD COLUMN` без rebuild (як у `027`): додається лише nullable-колонка,
 * що SQLite підтримує напряму. Жодних правок старих міграцій, жодних деструктивних змін.
 */
export const version = 28;

export async function up(db: SQLiteDatabase): Promise<void> {
  await db.execAsync(`
    ALTER TABLE reading_session ADD COLUMN started_calendar_date TEXT;
    ALTER TABLE reading_run ADD COLUMN started_calendar_date TEXT;
    ALTER TABLE reading_run ADD COLUMN finished_calendar_date TEXT;
  `);
}
